#ifndef OBJECT_TYPES
#define OBJECT_TYPES

// Define the possible type of an object in the game scene
typedef enum class ObjectTypes
{
    Other,
    Player,
    Enemy,
    Missile,
} ObjectTypes;

#endif