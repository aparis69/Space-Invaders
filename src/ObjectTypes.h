#ifndef OBJECT_TYPES
#define OBJECT_TYPES

// Define the possible actions return by a ReactToCollision

typedef enum class ObjectTypes
{
    Other,
    Player,
    Enemy,
    Missile,
} ObjectTypes;

#endif