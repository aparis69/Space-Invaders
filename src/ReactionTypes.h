#ifndef ACTION_TYPES
#define ACTION_TYPES

// Define the possible actions return by a ReactToCollision
typedef enum class ReactionTypes
{
    Destroy,
    ChangeDirection,
    Nothing,
} ReactionTypes;

#endif