#ifndef ACTION_TYPES
#define ACTION_TYPES

// Define the possible actions return by a ReactToCollision

typedef enum class ActionTypes
{
    Destroy,
    ChangeDirection,
    Nothing,
} ActionTypes;

#endif